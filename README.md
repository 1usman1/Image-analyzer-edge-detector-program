# Image-analyzer-edge-detector-program
This is an image analyzer software, coded with python, this software uses the "sobel kernel solution" to find and highlight edges of a picture. It uses sobel kernel solution to detect pixels that differ in brightness and the program highlights it with a white color, while the rest is colored black. The user interface is built using tkinter. 
